#ifndef CONFIG_H
#define CONFIG_H

#include "colors.h" /* CP_* values are defined in colors.h */

/* layout constants */
enum {
  MIN_ROWS = 10,
  MIN_COLS = 30,
  MIN_WIN_H = 5,
  MIN_WIN_W = 20,
  SIDE_MARGIN = 2,
  FOOTER_LINES = 2
};

#endif /* CONFIG_H */
